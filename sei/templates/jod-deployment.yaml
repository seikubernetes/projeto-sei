kind: Deployment
apiVersion: apps/v1
metadata:
  name: {{ .Values.jod.name }}
  labels:
    app: {{ .Values.jod.name }}
    appglobal: {{ .Release.Name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.jod.name }}
      deployment: {{ .Values.jod.name }}
    spec:
      containers:
        - name: {{ .Values.jod.name }}
          image:  {{ .Values.jod.image }}
          ports:
            - containerPort: 8080
              protocol: TCP
      restartPolicy: Always
      terminationGracePeriodSeconds: 30